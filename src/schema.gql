type User {
  name: String
}

type Query {
  me: User
  mentors: [User]
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  signIn(input: LoginInput): User
  signOut: ID
}

schema {
  query: Query
  mutation: Mutation
}
