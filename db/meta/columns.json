{
  "connect_google": {
    "columns": [
      "user_id",
      "google_id",
      "refresh_token",
      "access_token",
      "scopes",
      "calendar_id"
    ]
  },
  "invites": {
    "columns": [
      "id",
      "issuer",
      "email",
      "role",
      "redeemed",
      "issued",
      "email_id"
    ]
  },
  "lists": {
    "columns": [
      "id",
      "name",
      "description",
      "public",
      "illustration",
      "background_color",
      "text_color",
      "sort_pos"
    ]
  },
  "meetups": {
    "columns": [
      "slot_id",
      "status",
      "mentee_id",
      "message",
      "location",
      "gcal_user_event_id",
      "gcal_upframe_event_id"
    ]
  },
  "mentors": {
    "columns": [
      "id",
      "listed",
      "company",
      "slot_reminder_email",
      "score",
      "headline"
    ]
  },
  "profile_pictures": {
    "columns": ["user_id", "url", "size", "type"]
  },
  "signin_upframe": {
    "columns": ["email", "password", "user_id"]
  },
  "signup": {
    "columns": ["token", "started", "email", "password", "google_id"]
  },
  "time_slots": {
    "columns": ["id", "mentor_id", "start", "end"]
  },
  "socialmedia": {
    "columns": ["id", "name", "url"]
  },
  "spaces": {
    "columns": [
      "id",
      "name",
      "handle",
      "created",
      "description",
      "sidebar",
      "founder_invite",
      "mentor_invite",
      "owner_invite",
      "space_imgs",
      "cover_imgs"
    ]
  },
  "tags": {
    "columns": ["id", "name"]
  },
  "tokens": {
    "columns": ["token", "scope", "subject", "payload", "issued", "expires"]
  },
  "users": {
    "columns": [
      "id",
      "handle",
      "name",
      "email",
      "location",
      "biography",
      "allow_emails",
      "website",
      "role",
      "name_normalized",
      "searchable",
      "headline",
      "timezone",
      "tz_infer",
      "display_name",
      "joined",
      "msg_emails"
    ]
  },
  "user_handles": {
    "columns": ["user_id", "platform_id", "handle"]
  },
  "user_lists": {
    "columns": ["user_id", "list_id"]
  },
  "user_tags": {
    "columns": ["user_id", "tag_id"]
  }
}
